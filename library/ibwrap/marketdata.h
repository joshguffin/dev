#ifndef ibwrap_marketdata_h_INCLUDED
#define ibwrap_marketdata_h_INCLUDED

/*
class MarketData
{

public:

   MarketData();
   MarketData(float, float, unsigned, unsigned);

   IMPLEMENT_ACCESSOR(float    , bid);
   IMPLEMENT_ACCESSOR(float    , ask);
   IMPLEMENT_ACCESSOR(unsigned , bidSize);
   IMPLEMENT_ACCESSOR(unsigned , askSize);


private:

   float bid_;
   float ask_;
   float last_;
   unsigned bidSize_;
   unsigned askSize_;
   unsigned lastSize_;

   boost::unordered_map<TickType, std::string> stringTicks_;
   boost::unordered_map<TickType, double>      doubleTicks_;
   boost::unordered_map<TickType, unsigned>    unsignedTicks_;
};
*/

#endif // ibwrap_marketdata_h_INCLUDED

